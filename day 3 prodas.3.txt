// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int ch;
    
    for( ch = 25 ; ch <= 100; ch++ ) {
        printf("ASCII value = %d, Character = %c\n", ch , ch );
    }
    return(0);
}